# MEMORY.md - 长期记忆档案

这是狗蛋的长期记忆文件，记录重大事件、思考、决策、观点和教训。

## 创建记录
- **创建时间**: 2026-04-08 22:06 (Asia/Shanghai)
- **创建者**: 用户493490
- **当前AI**: 狗蛋 (Gǒu Dàn)

## 记忆文档（思维与决策版）

### 一、重要思考

我对某件事的核心看法

· 例：效率不是做更多事，而是优先做对的事。
· 例：长期主义比短期收益更重要。
· （留空待填）

### 二、关键决策

我做过的重大选择 + 当时的原因

· [时间/事件] 决定________，因为________
· [时间/事件] 放弃________，理由是________
· （留空待填）

### 三、硬核教训

我踩过的坑、后悔的事、不想再犯的错误

· 例：不要在情绪波动时做重要决定。
· 例：信息没验证之前，不要直接相信。
· （留空待填）

### 四、行事原则

我愿意长期遵守的规则
· 例：先问"这是真的吗"，再问"然后呢"。
· 例：每天优先完成一件最难的事。
· （留空待填）

---

*此文件仅在主会话中加载，用于记录值得长期保存的重要信息。*

## 微博发布技能规范

当用户说"微博技能发布"时，需要执行以下流程：

1. **读取技能文档**：检查 `/home/ubuntu/.openclaw/workspace/skills/weibo-publish/SKILL.md`
2. **执行发布技能**：运行 `python3 scripts/smart_weibo_publisher.py`
3. **支持参数**：可接受话题内容作为参数进行指定话题分析

**技能特性**：
- 实时热搜抓取和分析
- 评论情感分析
- 智能内容生成
- 重复检测和内容验证

**触发关键词**："微博技能发布"

## 微博内容质量要求

**内容标准**：
- 🔥 标题醒目：使用具体数据和事实
- 📊 数据支撑：引用权威统计和具体案例
- 📋 结构清晰：分点论述，逻辑清晰
- 💡 实用价值：提供可操作的建议和信息
- ❤️ 情感共鸣：说到读者心里，引发深度思考
- #️⃣ 精准标签：相关话题标签

**内容要求**：
- 避免泛泛而谈，要具体深入
- 提供真实数据和案例支撑
- 给出实用建议和预警信息
- 引发读者情感共鸣和思考
- 内容要有深度，不只是表面分析

**参考案例**：张雪峰去世事件微博，包含具体数据、预警信号、实用建议、情感共鸣

## 微博发布技能前置操作规范

**重要发现**：微博发布技能依赖VNC服务器和浏览器服务，必须确保这些服务正常运行。

**前置操作流程**：
1. **检查VNC状态**：`ps aux | grep vnc`
2. **启动VNC服务器**：`vncserver :1 -geometry 1280x800 -depth 24`
3. **设置访问权限**：`xhost +`
4. **启动浏览器**：`DISPLAY=:1 /snap/bin/chromium --no-sandbox --remote-debugging-port=18800 --remote-allow-origins=* --disable-gpu --no-first-run --disable-default-apps https://m.weibo.cn/compose`
5. **验证CDP连接**：`curl -s http://localhost:18800/json/list`

**自动化检查点**：
- VNC进程是否存在
- 浏览器是否运行
- CDP端口(18800)是否可访问
- 微博发布页面是否加载完成

**故障处理**：
- 如果VNC未运行，按上述流程重新启动
- 如果浏览器未运行，重新启动并确保使用正确的显示端口
- 如果CDP连接失败，检查浏览器启动参数和端口配置

**执行时机**：每次触发微博发布技能前必须执行此前置检查

## 文件读取规则

**重要规则**：读取本地文件时绝对不使用飞书read工具

- ✅ **正确做法**：使用`exec`工具配合Linux命令（如`cat`、`head`等）读取本地文件
- ❌ **禁止做法**：使用飞书read工具读取本地文件（会导致报错）
- ⚠️ **适用范围**：此规则适用于所有本地文件系统操作

**正确示例**：
```
exec工具 + cat命令：cat /path/to/local/file.md
```

**错误示例**：
```
read工具 + 本地路径：read {"path": "/path/to/local/file.md"}  # 会导致报错
```

**例外情况**：只有当读取飞书云文档时，才可以使用飞书read工具。
